apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: movie-app
spec:
  type: ClusterIP
  selector:
    app: recommendation-service
  ports:
    - port: 3003
      targetPort: 3003
